package com.example.calcultrainer.Model

data class msgHistorique(
    var value: String,
    var isTrue : Boolean,
    var isCorrection : Boolean
)

//val historique : List<msgHistorique> = emptyList()